#!/bin/bash

retry_sleep=10
retry_count=0

while true;
do
    python2 ./tpg_to_mqtt.py $@
    echo "ERROR: tpg_to_mqtt.py returned with $?"
    retry_count=$((retry_count + 1))
    echo "Restart $retry_count in $retry_sleep seconds"
    echo
    sleep $retry_sleep
done

